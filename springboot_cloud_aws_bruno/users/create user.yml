info:
  name: create user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/users/
  body:
    type: json
    data: |-
      {
        "username": "testing_user docker",
        "password": "123456789",
        "email": "testinxxg@hotmail.com",
        "isAdmin": true
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
