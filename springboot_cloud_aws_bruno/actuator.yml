info:
  name: actuator
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8081/actuator/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
