info:
  name: login
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8093/login
  body:
    type: form-urlencoded
    data:
      - name: username
        value: user
      - name: password
        value: password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
