info:
  name: (1 - servlet) get login route copy
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:8080/login/oauth2/code/oidc-client
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
