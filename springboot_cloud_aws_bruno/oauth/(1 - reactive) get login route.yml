info:
  name: (1 - reactive) get login route
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8080/oauth2/authorization/oidc-client
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
