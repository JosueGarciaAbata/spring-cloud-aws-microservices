info:
  name: (2) log in
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8093/oauth2/authorization/oidc-client
  body:
    type: form-urlencoded
    data:
      - name: username
        value: admin
      - name: password
        value: password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
