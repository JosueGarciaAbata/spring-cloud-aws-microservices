info:
  name: get user with token
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/v1/users/

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
