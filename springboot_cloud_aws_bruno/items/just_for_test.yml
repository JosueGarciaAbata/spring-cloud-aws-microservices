info:
  name: just_for_test
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/api/v1/items/11
  body:
    type: json
    data: |-
      {
        "name": "product from ddddd",
        "price": 100.0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
