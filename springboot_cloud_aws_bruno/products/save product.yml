info:
  name: save product
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/v1/products/
  body:
    type: json
    data: |-
      {
        "name": "new product - docker",
        "price": 100.0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
