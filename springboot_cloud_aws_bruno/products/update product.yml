info:
  name: update product
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/v1/products/2
  body:
    type: json
    data: |-
      {
        "name": "docker compose -rr",
        "price": 20.90
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
